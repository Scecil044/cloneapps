version: '3'

services:
  node-app:
    build: .
    image: node_api
    ports:
      - '5100:5100'
#    depends_on:
#      - mongodb
    volumes:
      - .:/usr/src/api-dir
    networks:
      - node-network

#  mongodb:
#    image: mongo:4.2.1-bionic
#    ports:
#      - '27017:27017'
#    volumes:
#      - dbdata:/data/db
#    networks:
#      - node-network

volumes:
  dbdata:

networks:
  node-network:
    driver: bridge
